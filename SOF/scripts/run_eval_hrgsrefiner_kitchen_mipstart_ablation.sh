#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
SOF_ROOT="$(cd -- "${SCRIPT_DIR}/.." && pwd)"

ABLATION_MODE="${ABLATION_MODE:-baseline_action}"

case "${ABLATION_MODE}" in
  baseline_action)
    : "${CHECKPOINT_TAG:=mipstart_action_probe}"
    : "${USE_ACTION_PAYLOAD:=1}"
    : "${USE_ROUTE_PAYLOAD:=0}"
    : "${GAUSSIAN_ACTION_UPDATE_SCALE:=1.0}"
    : "${GAUSSIAN_ACTION_ATTACH_SCALE:=1.0}"
    : "${GAUSSIAN_ACTION_DETAIL_SCALE:=1.0}"
    : "${GAUSSIAN_ACTION_PRIOR_COLOR_SCALE:=1.0}"
    : "${FREEZE_DETAIL_GEOMETRY:=0}"
    ;;
  no_action)
    : "${CHECKPOINT_TAG:=mipstart_noaction_probe}"
    : "${USE_ACTION_PAYLOAD:=0}"
    : "${USE_ROUTE_PAYLOAD:=0}"
    : "${FREEZE_DETAIL_GEOMETRY:=0}"
    ;;
  weak_action)
    : "${CHECKPOINT_TAG:=mipstart_weakaction_probe}"
    : "${USE_ACTION_PAYLOAD:=1}"
    : "${USE_ROUTE_PAYLOAD:=0}"
    : "${GAUSSIAN_ACTION_UPDATE_SCALE:=0.25}"
    : "${GAUSSIAN_ACTION_ATTACH_SCALE:=0.25}"
    : "${GAUSSIAN_ACTION_DETAIL_SCALE:=0.25}"
    : "${GAUSSIAN_ACTION_PRIOR_COLOR_SCALE:=0.25}"
    : "${FREEZE_DETAIL_GEOMETRY:=0}"
    ;;
  prior_color_off)
    : "${CHECKPOINT_TAG:=mipstart_priorcoloroff_probe}"
    : "${USE_ACTION_PAYLOAD:=1}"
    : "${USE_ROUTE_PAYLOAD:=0}"
    : "${GAUSSIAN_ACTION_UPDATE_SCALE:=1.0}"
    : "${GAUSSIAN_ACTION_ATTACH_SCALE:=1.0}"
    : "${GAUSSIAN_ACTION_DETAIL_SCALE:=1.0}"
    : "${GAUSSIAN_ACTION_PRIOR_COLOR_SCALE:=0.0}"
    : "${FREEZE_DETAIL_GEOMETRY:=0}"
    ;;
  freeze_detail_geometry)
    : "${CHECKPOINT_TAG:=mipstart_freezedetail_probe}"
    : "${USE_ACTION_PAYLOAD:=1}"
    : "${USE_ROUTE_PAYLOAD:=0}"
    : "${GAUSSIAN_ACTION_UPDATE_SCALE:=1.0}"
    : "${GAUSSIAN_ACTION_ATTACH_SCALE:=1.0}"
    : "${GAUSSIAN_ACTION_DETAIL_SCALE:=1.0}"
    : "${GAUSSIAN_ACTION_PRIOR_COLOR_SCALE:=1.0}"
    : "${FREEZE_DETAIL_GEOMETRY:=1}"
    ;;
  weak_action_freeze_detail)
    : "${CHECKPOINT_TAG:=mipstart_weakaction_freezedetail_probe}"
    : "${USE_ACTION_PAYLOAD:=1}"
    : "${USE_ROUTE_PAYLOAD:=0}"
    : "${GAUSSIAN_ACTION_UPDATE_SCALE:=0.25}"
    : "${GAUSSIAN_ACTION_ATTACH_SCALE:=0.25}"
    : "${GAUSSIAN_ACTION_DETAIL_SCALE:=0.25}"
    : "${GAUSSIAN_ACTION_PRIOR_COLOR_SCALE:=0.25}"
    : "${FREEZE_DETAIL_GEOMETRY:=1}"
    ;;
  route_v0|route_v0p5|route_v0p5_detail_preserve)
    : "${CHECKPOINT_TAG:=mipstart_routev0p5_probe}"
    : "${USE_ACTION_PAYLOAD:=1}"
    : "${USE_ROUTE_PAYLOAD:=1}"
    : "${ROUTE_EXECUTION_MODE:=detail_preserve_v0p5}"
    : "${ROUTE_DETAIL_PROTECT_BETA:=0.7}"
    : "${ROUTE_DETAIL_ATTACH_STRENGTH:=0.0}"
    : "${ROUTE_DETAIL_GEOMETRY_STRENGTH:=0.0}"
    : "${ROUTE_PRIOR_COLOR_STRENGTH_SCALE:=0.25}"
    : "${ROUTE_SUPPRESS_UPDATE_FLOOR:=0.15}"
    : "${ROUTE_SUPPRESS_OPACITY_SCALE:=0.25}"
    : "${ROUTE_MIN_SCALE_GATE:=0.3}"
    : "${GAUSSIAN_ACTION_UPDATE_SCALE:=1.0}"
    : "${GAUSSIAN_ACTION_ATTACH_SCALE:=1.0}"
    : "${GAUSSIAN_ACTION_DETAIL_SCALE:=1.0}"
    : "${GAUSSIAN_ACTION_PRIOR_COLOR_SCALE:=1.0}"
    : "${FREEZE_DETAIL_GEOMETRY:=1}"
    ;;
  route_v0p6|route_v0p6_detail_preserve)
    : "${CHECKPOINT_TAG:=mipstart_routev0p6_probe}"
    : "${USE_ACTION_PAYLOAD:=1}"
    : "${USE_ROUTE_PAYLOAD:=1}"
    : "${ROUTE_EXECUTION_MODE:=detail_preserve_v0p6}"
    : "${ROUTE_DETAIL_PROTECT_BETA:=0.7}"
    : "${ROUTE_DETAIL_ATTACH_STRENGTH:=0.0}"
    : "${ROUTE_DETAIL_GEOMETRY_STRENGTH:=0.0}"
    : "${ROUTE_PRIOR_COLOR_STRENGTH_SCALE:=0.25}"
    : "${ROUTE_SUPPRESS_UPDATE_FLOOR:=0.15}"
    : "${ROUTE_SUPPRESS_OPACITY_SCALE:=0.25}"
    : "${ROUTE_MIN_SCALE_GATE:=0.3}"
    : "${ROUTE_SCALE_GATE_SUPPRESS_COUPLING:=1.0}"
    : "${GAUSSIAN_ACTION_UPDATE_SCALE:=1.0}"
    : "${GAUSSIAN_ACTION_ATTACH_SCALE:=1.0}"
    : "${GAUSSIAN_ACTION_DETAIL_SCALE:=1.0}"
    : "${GAUSSIAN_ACTION_PRIOR_COLOR_SCALE:=1.0}"
    : "${FREEZE_DETAIL_GEOMETRY:=1}"
    ;;
  route_v0p7|route_v0p7_detail_preserve)
    : "${CHECKPOINT_TAG:=mipstart_routev0p7_probe}"
    : "${USE_ACTION_PAYLOAD:=1}"
    : "${USE_ROUTE_PAYLOAD:=1}"
    : "${ROUTE_EXECUTION_MODE:=detail_preserve_v0p7}"
    : "${ROUTE_DETAIL_PROTECT_BETA:=0.7}"
    : "${ROUTE_DETAIL_ATTACH_STRENGTH:=0.0}"
    : "${ROUTE_DETAIL_GEOMETRY_STRENGTH:=0.0}"
    : "${ROUTE_PRIOR_COLOR_STRENGTH_SCALE:=0.25}"
    : "${ROUTE_SUPPRESS_UPDATE_FLOOR:=0.15}"
    : "${ROUTE_SUPPRESS_OPACITY_SCALE:=0.25}"
    : "${ROUTE_MIN_SCALE_GATE:=0.3}"
    : "${ROUTE_SCALE_GATE_SUPPRESS_COUPLING:=1.0}"
    : "${GAUSSIAN_ACTION_UPDATE_SCALE:=1.0}"
    : "${GAUSSIAN_ACTION_ATTACH_SCALE:=1.0}"
    : "${GAUSSIAN_ACTION_DETAIL_SCALE:=1.0}"
    : "${GAUSSIAN_ACTION_PRIOR_COLOR_SCALE:=1.0}"
    : "${FREEZE_DETAIL_GEOMETRY:=1}"
    ;;
  route_v0p8|route_v0p8_detail_preserve)
    : "${CHECKPOINT_TAG:=mipstart_routev0p8_probe}"
    : "${USE_ACTION_PAYLOAD:=1}"
    : "${USE_ROUTE_PAYLOAD:=1}"
    : "${ROUTE_EXECUTION_MODE:=detail_preserve_v0p8}"
    : "${ROUTE_DETAIL_PROTECT_BETA:=0.7}"
    : "${ROUTE_DETAIL_ATTACH_STRENGTH:=0.0}"
    : "${ROUTE_DETAIL_GEOMETRY_STRENGTH:=0.0}"
    : "${ROUTE_PRIOR_COLOR_STRENGTH_SCALE:=0.25}"
    : "${ROUTE_SUPPRESS_UPDATE_FLOOR:=0.15}"
    : "${ROUTE_SUPPRESS_OPACITY_SCALE:=0.25}"
    : "${ROUTE_MIN_SCALE_GATE:=0.3}"
    : "${ROUTE_SCALE_GATE_SUPPRESS_COUPLING:=1.0}"
    : "${ROUTE_RISKY_USEFUL_OPACITY_COUPLING:=0.35}"
    : "${ROUTE_RISKY_USEFUL_SCALE_COUPLING:=0.2}"
    : "${ROUTE_HARMFUL_SCALE_COUPLING:=1.0}"
    : "${GAUSSIAN_ACTION_UPDATE_SCALE:=1.0}"
    : "${GAUSSIAN_ACTION_ATTACH_SCALE:=1.0}"
    : "${GAUSSIAN_ACTION_DETAIL_SCALE:=1.0}"
    : "${GAUSSIAN_ACTION_PRIOR_COLOR_SCALE:=1.0}"
    : "${FREEZE_DETAIL_GEOMETRY:=1}"
    ;;
  route_v0_freeze_detail|route_v0p5_freeze_detail)
    : "${CHECKPOINT_TAG:=mipstart_routev0p5_freezedetail_probe}"
    : "${USE_ACTION_PAYLOAD:=1}"
    : "${USE_ROUTE_PAYLOAD:=1}"
    : "${ROUTE_EXECUTION_MODE:=detail_preserve_v0p5}"
    : "${ROUTE_DETAIL_PROTECT_BETA:=0.7}"
    : "${ROUTE_DETAIL_ATTACH_STRENGTH:=0.0}"
    : "${ROUTE_DETAIL_GEOMETRY_STRENGTH:=0.0}"
    : "${ROUTE_PRIOR_COLOR_STRENGTH_SCALE:=0.25}"
    : "${ROUTE_SUPPRESS_UPDATE_FLOOR:=0.15}"
    : "${ROUTE_SUPPRESS_OPACITY_SCALE:=0.25}"
    : "${ROUTE_MIN_SCALE_GATE:=0.3}"
    : "${GAUSSIAN_ACTION_UPDATE_SCALE:=1.0}"
    : "${GAUSSIAN_ACTION_ATTACH_SCALE:=1.0}"
    : "${GAUSSIAN_ACTION_DETAIL_SCALE:=1.0}"
    : "${GAUSSIAN_ACTION_PRIOR_COLOR_SCALE:=1.0}"
    : "${FREEZE_DETAIL_GEOMETRY:=1}"
    ;;
  route_v0p5_suppress_only)
    : "${CHECKPOINT_TAG:=mipstart_routev0p5_suppressonly_probe}"
    : "${USE_ACTION_PAYLOAD:=1}"
    : "${USE_ROUTE_PAYLOAD:=1}"
    : "${ROUTE_EXECUTION_MODE:=suppress_only_v0p5}"
    : "${ROUTE_DETAIL_PROTECT_BETA:=0.7}"
    : "${ROUTE_DETAIL_ATTACH_STRENGTH:=0.0}"
    : "${ROUTE_DETAIL_GEOMETRY_STRENGTH:=0.0}"
    : "${ROUTE_PRIOR_COLOR_STRENGTH_SCALE:=0.0}"
    : "${ROUTE_SUPPRESS_UPDATE_FLOOR:=0.15}"
    : "${ROUTE_SUPPRESS_OPACITY_SCALE:=0.25}"
    : "${ROUTE_MIN_SCALE_GATE:=0.3}"
    : "${GAUSSIAN_ACTION_UPDATE_SCALE:=1.0}"
    : "${GAUSSIAN_ACTION_ATTACH_SCALE:=1.0}"
    : "${GAUSSIAN_ACTION_DETAIL_SCALE:=1.0}"
    : "${GAUSSIAN_ACTION_PRIOR_COLOR_SCALE:=1.0}"
    : "${FREEZE_DETAIL_GEOMETRY:=1}"
    ;;
  route_v0p6_suppress_only)
    : "${CHECKPOINT_TAG:=mipstart_routev0p6_suppressonly_probe}"
    : "${USE_ACTION_PAYLOAD:=1}"
    : "${USE_ROUTE_PAYLOAD:=1}"
    : "${ROUTE_EXECUTION_MODE:=suppress_only_v0p6}"
    : "${ROUTE_DETAIL_PROTECT_BETA:=0.7}"
    : "${ROUTE_DETAIL_ATTACH_STRENGTH:=0.0}"
    : "${ROUTE_DETAIL_GEOMETRY_STRENGTH:=0.0}"
    : "${ROUTE_PRIOR_COLOR_STRENGTH_SCALE:=0.0}"
    : "${ROUTE_SUPPRESS_UPDATE_FLOOR:=0.15}"
    : "${ROUTE_SUPPRESS_OPACITY_SCALE:=0.25}"
    : "${ROUTE_MIN_SCALE_GATE:=0.3}"
    : "${ROUTE_SCALE_GATE_SUPPRESS_COUPLING:=1.0}"
    : "${GAUSSIAN_ACTION_UPDATE_SCALE:=1.0}"
    : "${GAUSSIAN_ACTION_ATTACH_SCALE:=1.0}"
    : "${GAUSSIAN_ACTION_DETAIL_SCALE:=1.0}"
    : "${GAUSSIAN_ACTION_PRIOR_COLOR_SCALE:=1.0}"
    : "${FREEZE_DETAIL_GEOMETRY:=1}"
    ;;
  route_v0p7_suppress_only)
    : "${CHECKPOINT_TAG:=mipstart_routev0p7_suppressonly_probe}"
    : "${USE_ACTION_PAYLOAD:=1}"
    : "${USE_ROUTE_PAYLOAD:=1}"
    : "${ROUTE_EXECUTION_MODE:=suppress_only_v0p7}"
    : "${ROUTE_DETAIL_PROTECT_BETA:=0.7}"
    : "${ROUTE_DETAIL_ATTACH_STRENGTH:=0.0}"
    : "${ROUTE_DETAIL_GEOMETRY_STRENGTH:=0.0}"
    : "${ROUTE_PRIOR_COLOR_STRENGTH_SCALE:=0.0}"
    : "${ROUTE_SUPPRESS_UPDATE_FLOOR:=0.15}"
    : "${ROUTE_SUPPRESS_OPACITY_SCALE:=0.25}"
    : "${ROUTE_MIN_SCALE_GATE:=0.3}"
    : "${ROUTE_SCALE_GATE_SUPPRESS_COUPLING:=1.0}"
    : "${GAUSSIAN_ACTION_UPDATE_SCALE:=1.0}"
    : "${GAUSSIAN_ACTION_ATTACH_SCALE:=1.0}"
    : "${GAUSSIAN_ACTION_DETAIL_SCALE:=1.0}"
    : "${GAUSSIAN_ACTION_PRIOR_COLOR_SCALE:=1.0}"
    : "${FREEZE_DETAIL_GEOMETRY:=1}"
    ;;
  route_v0p8_suppress_only)
    : "${CHECKPOINT_TAG:=mipstart_routev0p8_suppressonly_probe}"
    : "${USE_ACTION_PAYLOAD:=1}"
    : "${USE_ROUTE_PAYLOAD:=1}"
    : "${ROUTE_EXECUTION_MODE:=suppress_only_v0p8}"
    : "${ROUTE_DETAIL_PROTECT_BETA:=0.7}"
    : "${ROUTE_DETAIL_ATTACH_STRENGTH:=0.0}"
    : "${ROUTE_DETAIL_GEOMETRY_STRENGTH:=0.0}"
    : "${ROUTE_PRIOR_COLOR_STRENGTH_SCALE:=0.0}"
    : "${ROUTE_SUPPRESS_UPDATE_FLOOR:=0.15}"
    : "${ROUTE_SUPPRESS_OPACITY_SCALE:=0.25}"
    : "${ROUTE_MIN_SCALE_GATE:=0.3}"
    : "${ROUTE_SCALE_GATE_SUPPRESS_COUPLING:=1.0}"
    : "${ROUTE_RISKY_USEFUL_OPACITY_COUPLING:=0.35}"
    : "${ROUTE_RISKY_USEFUL_SCALE_COUPLING:=0.2}"
    : "${ROUTE_HARMFUL_SCALE_COUPLING:=1.0}"
    : "${GAUSSIAN_ACTION_UPDATE_SCALE:=1.0}"
    : "${GAUSSIAN_ACTION_ATTACH_SCALE:=1.0}"
    : "${GAUSSIAN_ACTION_DETAIL_SCALE:=1.0}"
    : "${GAUSSIAN_ACTION_PRIOR_COLOR_SCALE:=1.0}"
    : "${FREEZE_DETAIL_GEOMETRY:=1}"
    ;;
  *)
    echo "[eval-hrgs-mipstart-ablation] unsupported ABLATION_MODE=${ABLATION_MODE}" >&2
    echo "supported: baseline_action, no_action, weak_action, prior_color_off, freeze_detail_geometry, weak_action_freeze_detail, route_v0, route_v0_freeze_detail, route_v0p5, route_v0p5_detail_preserve, route_v0p5_freeze_detail, route_v0p5_suppress_only, route_v0p6, route_v0p6_detail_preserve, route_v0p6_suppress_only, route_v0p7, route_v0p7_detail_preserve, route_v0p7_suppress_only, route_v0p8, route_v0p8_detail_preserve, route_v0p8_suppress_only" >&2
    exit 1
    ;;
esac

echo "[eval-hrgs-mipstart-ablation] mode                 : ${ABLATION_MODE}"
echo "[eval-hrgs-mipstart-ablation] checkpoint tag       : ${CHECKPOINT_TAG}"
echo "[eval-hrgs-mipstart-ablation] use action payload   : ${USE_ACTION_PAYLOAD}"
echo "[eval-hrgs-mipstart-ablation] use route payload    : ${USE_ROUTE_PAYLOAD}"
echo "[eval-hrgs-mipstart-ablation] action scales        : update=${GAUSSIAN_ACTION_UPDATE_SCALE} attach=${GAUSSIAN_ACTION_ATTACH_SCALE} detail=${GAUSSIAN_ACTION_DETAIL_SCALE} prior=${GAUSSIAN_ACTION_PRIOR_COLOR_SCALE}"
if [[ "${USE_ROUTE_PAYLOAD}" == "1" ]]; then
  echo "[eval-hrgs-mipstart-ablation] route exec mode      : ${ROUTE_EXECUTION_MODE}"
fi
echo "[eval-hrgs-mipstart-ablation] freeze detail geom   : ${FREEZE_DETAIL_GEOMETRY}"

bash "${SOF_ROOT}/scripts/run_eval_hrgsrefiner_kitchen_mipstart.sh"
